FROM node:22-alpine AS build
WORKDIR /app
COPY viva/package*.json ./
RUN npm ci
COPY viva/ .
RUN npm run build
FROM nginxinc/nginx-unprivileged:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY deploy/nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 8080
